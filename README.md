# Rails Blog App

Rails refresher

#### Setup

* Clone this repository
* Install all dependencies with ```bundle install```
* ```bin/rails db:create```
* ```bin/rails db:migrate```
* Run ``` bin/rails server ``` and access the blog on ```localhost:3000```
